export type CardDetails = {
  textColor?: string;
  secondaryTextColor?: string;
  backgroundColor: string;
  cardHolder: string;
  finalNumbers: string;
  expiration: string;
  cardBrand: string;
};
