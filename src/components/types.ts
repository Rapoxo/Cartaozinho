export type CardDetails = {
  template?: boolean
  textColor?: string;
  secondaryTextColor?: string;
  backgroundColor: string;
  cardHolder: string;
  finalNumbers: string;
  expiration: string;
  cardBrand: string;
  totalAmount: number;
};
